---
name: gemini-review
description: Gemini에게 코드 리뷰를 요청하고, 피드백을 바탕으로 Claude Code가 코드를 개선합니다
user-invocable: true
allowed-tools: Bash, Read, Write, Edit, Grep, Glob
argument-hint: "[파일경로 또는 git diff]"
---

# Gemini Review - Gemini가 리뷰, Claude가 수정

코드 리뷰를 Gemini에게 요청하고, 리뷰 결과를 바탕으로 Claude Code가 코드를 개선합니다.

## 워크플로우

### 1단계: 리뷰 대상 수집

`$ARGUMENTS`에 따라 리뷰 대상을 결정:

- **파일 경로 지정**: 해당 파일 내용을 읽음
- **인자 없음**: `git diff --cached` (staged 변경) 또는 `git diff` (unstaged 변경)
- **PR 번호**: `gh pr diff $PR_NUMBER`

```bash
# staged 변경이 있으면 staged, 없으면 unstaged
DIFF=$(git diff --cached)
if [ -z "$DIFF" ]; then
  DIFF=$(git diff)
fi
```

### 2단계: Gemini에게 리뷰 요청

```bash
echo "$CODE_TO_REVIEW" | gemini -p "
당신은 시니어 코드 리뷰어입니다.
다음 코드를 리뷰하고 구체적인 개선점을 제시해주세요.

## 리뷰 기준
1. **버그/로직 오류**: 잠재적 버그, 엣지 케이스 미처리
2. **보안**: 인젝션, XSS, 인증/인가 문제
3. **성능**: 불필요한 연산, N+1 쿼리, 메모리 누수
4. **가독성**: 네이밍, 구조, 복잡도
5. **베스트 프랙티스**: 언어/프레임워크 관례 준수

## 응답 형식
각 이슈를 다음 형식으로:

### [심각도: 🔴critical | 🟡warning | 🔵suggestion]
- **파일**: 파일명:라인번호
- **문제**: 문제 설명
- **수정 제안**: 구체적인 코드 변경 제안

마지막에 전체 요약과 점수(10점 만점)를 부여해주세요.
"
```

### 3단계: 리뷰 결과 표시

Gemini의 리뷰 결과를 사용자에게 보여줍니다.

### 4단계: 자동 수정 (사용자 승인 시)

사용자에게 물어봅니다:
> Gemini가 N개의 이슈를 발견했습니다. 자동으로 수정할까요?
> - `critical` 이슈 N개
> - `warning` 이슈 N개
> - `suggestion` N개

승인하면 Claude Code가 Gemini의 제안을 바탕으로 코드를 수정합니다.

### 5단계: 결과 보고

```
📝 Gemini 코드 리뷰 결과
━━━━━━━━━━━━━━━━━━━━━━━━

점수: 7/10

발견된 이슈:
  🔴 Critical: N개
  🟡 Warning:  N개
  🔵 Suggestion: N개

자동 수정:
  ✅ 수정 완료: N개
  ⏭️ 건너뜀: N개 (사용자 판단 필요)
```

## 사용 예시

```
/gemini-review                      # staged/unstaged 변경 리뷰
/gemini-review src/auth/login.ts    # 특정 파일 리뷰
/gemini-review --pr 42              # PR #42 리뷰
```
